# snippets for creating snippets

snippet "ts (\d+) (.*)" "make default tex snippet" r
snippet ${1} `!p
n = int(match.group(1))
command = match.group(2)
snip.rv  = '"\\' + command + n*"{}" + '"\n'
snip.rv += '\\' + command
for i in range(1,n+1):
	if i==1:
		snip.rv += "{${1:${VISUAL}}}"
	else:
		snip.rv += "{$" + str(i) + "}"
snip.rv += "\n"
snip.rv += "endsnippet\n"
`${0}
endsnippet
